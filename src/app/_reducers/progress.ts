export const progress = ( state = [], action) => {
    switch( action.type ) {
        case 'CHANGE_COLOR':
          return state;
        case 'CHANGE_PERCENT':
          return state;

        default:
          return state;
    }
}
